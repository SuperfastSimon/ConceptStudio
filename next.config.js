/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable if deploying to Vercel with Edge Functions
  // experimental: { serverActions: true },
}

module.exports = nextConfig